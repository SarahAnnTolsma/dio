[package]
name = "dio-wasm"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "WASM bindings for dio JavaScript deobfuscator"

[lib]
crate-type = ["cdylib"]

[dependencies]
dio-core = { path = "../dio-core" }
wasm-bindgen = "0.2"
